import { defineConfig } from "@eslint/config-helpers";

export default defineConfig([
  {
    languageOptions: {
      globals: {},
      parserOptions: {}
    },
  },
  {
    files: ["*.test.js"],
    ignores: ["*.fixture.js", "**/snapshots/**"],
    languageOptions: {
      globals: {},
      parserOptions: {}
    },
    rules: {
      "no-console": "warn"
    },
  }
]);
